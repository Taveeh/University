#!/usr/local/bin/bash

declare -A strings
n=0
ok=1
while read str; do
	for elem in "${strings[@]}"; do
		if [ $elem == $str ]; then
			ok=2
			break
		fi
	done
	if [ $ok -eq 2 ]; then
		break
	fi
	strings[$n]=$str
	n=$(($n + 1))
done
for i in ${strings[@]}; do
	echo $i
done


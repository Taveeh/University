#!/usr/local/bin/bash

for file in $(find ./utils); do
	if [ -f "$file" ]; then
		hasWrite=$(ls -l "$file" | grep "^.\{8\}w.*" -c)
		if [ $hasWrite -eq 1 ]; then
			echo "$(ls -l "$file")"
			$(chmod o-w "$file")
			echo "$(ls -l "$file")"
			$(chmod o+w "$file")
		fi
	fi
done

